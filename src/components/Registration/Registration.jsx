import {Auth} from '../Auth/Auth';

export const Registration = () => (
  <div className="registration">
    <div className="registration__content">
      <h1 className="registration__title">Sign up to App</h1>

      <Auth />
    </div>

    <div className="registration__wrapp"></div>
  </div>
)
